{
  "dependencies": {
    "csv-parser": "^1.9.3"
  },
  "engines": {
    "node": ">=7.10"
  },
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server"
  }
}
